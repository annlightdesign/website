const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { console.log('Connecting...'); await prisma.$executeRawUnsafe("SELECT pg_terminate_backend(pg_stat_activity.pid) FROM pg_stat_activity WHERE pg_stat_activity.datname = 'defaultdb' AND pid <> pg_backend_pid();"); console.log('Successfully killed all ghost connections!'); } main().catch(e => console.error('Script error:', e)).finally(() => prisma.$disconnect());
